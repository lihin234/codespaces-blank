[variables]
PIP_BREAK_SYSTEM_PACKAGES = "1"[phases.setup]
nixPkgs =["nodejs_20", "python3", "python3Packages.pip", "gcc"]

[phases.install]
cmds =[
  "npm install",
  "pip3 install -r requirements.txt"
]

[start]
cmd = "node bridge.js & python3 guardian_bot.py"
